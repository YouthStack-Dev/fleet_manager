# Environment Configuration Examples

# ===========================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ===========================================
ENV=development
DEBUG=true

# Storage - Local Development Filesystem
STORAGE_TYPE=filesystem
LOCAL_DEV_STORAGE_PATH=./local_storage
MAX_FILE_SIZE_MB=5

# Database - Local
DATABASE_URL=postgresql://fleetadmin:fleetpass@localhost:5432/fleet_db

# ===========================================
# DEV SERVER ENVIRONMENT (Linux Server)
# ===========================================
ENV=dev-server
DEBUG=true

# Storage - Dev Server Filesystem
STORAGE_TYPE=filesystem
DEV_SERVER_STORAGE_PATH=/var/lib/fleet/dev-storage
MAX_FILE_SIZE_MB=5

# Database - Dev Server
DATABASE_URL=postgresql://fleetadmin:fleetpass@fleet_postgres_dev:5432/fleet_db_dev

# ===========================================
# PRODUCTION ENVIRONMENT - FILESYSTEM
# ===========================================
ENV=production
DEBUG=false

# Storage - Production Server Filesystem (before S3 migration)
STORAGE_TYPE=filesystem
PROD_SERVER_STORAGE_PATH=/var/lib/fleet/storage
MAX_FILE_SIZE_MB=10

# Database - Production
DATABASE_URL=postgresql://fleetadmin:fleetpass@fleet_postgres:5432/fleet_db

# ===========================================
# PRODUCTION ENVIRONMENT - AWS S3 (After Migration)
# ===========================================
ENV=production
DEBUG=false

# Storage - AWS S3 (after migration)
STORAGE_TYPE=s3
S3_STORAGE_URL=s3://your-fleet-bucket/documents
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_DEFAULT_REGION=us-east-1
MAX_FILE_SIZE_MB=10

# Database - Production
DATABASE_URL=postgresql://fleetadmin:fleetpass@fleet_postgres:5432/fleet_db

# ===========================================
# CLOUD STORAGE EXAMPLES (Future Options)
# ===========================================

# Google Cloud Storage
# STORAGE_TYPE=gcs
# GCS_STORAGE_URL=gcs://your-fleet-bucket/documents
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Azure Blob Storage
# STORAGE_TYPE=azure
# AZURE_STORAGE_URL=abfs://container@account.dfs.core.windows.net/documents
# AZURE_STORAGE_ACCOUNT_NAME=your-account
# AZURE_STORAGE_ACCOUNT_KEY=your-key
